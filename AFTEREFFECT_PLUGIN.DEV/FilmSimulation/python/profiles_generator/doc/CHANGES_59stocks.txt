[STATUS NOTE 2026-08-02: historical changelog, kept verbatim. The
SOVCOLOR_DS_4 proposed below was a [T3] reconstruction for an earlier file
set and never entered the current database; the stock now exists as
SVEMA_DS_4, rebuilt [T2] from the printed table in Gurlev 1986 pp354-355
(S 45 GOST, gamma 0.8, L 1.2, Dmax 2, D0 0.25, R 63 lin/mm). Current
database: 89 stocks. See CHANGES_2026-08-02_soviet.md and
SOVIET_EXTRACTION_2026-08-02.md.]

CHANGES -- 56 -> 59 STOCKS (applied to YOUR uploaded schema-v2 files)
=====================================================================

NEW: SOVCOLOR_DS_4  (ДС-4)                                        [T3]
  Soviet colour negative, daylight 5600 K (your figure), 35 mm, UNMASKED.
  The missing mask is modelled, not just mentioned:
    - schema v2 gained an _UNMASKED set; _apply_schema_v2 now emits
      mask_encoding="none" for colour negatives in it (previously only
      dmin_ladder | neutral_dmin existed -- no way to say "no mask").
    - near-equal per-channel dmin (0.14/0.15/0.17) -- no orange ladder
    - grey base_tint (1.0/0.995/0.98), not orange
    - dye impurity UNcorrected: _dye(0.30) -- the muted crossed Sovcolor
      palette is dye impurity rendered honestly
  EI 40 is an estimate (GOST 32-45 class): no datasheet found. Aliases
  include Cyrillic "дс-4"/"дс4"/"совколор" -- your _norm() keeps Unicode
  alphanumerics, so they resolve. The "sovcolor" alias MOVED here from
  AGFACOLOR_NEU_1936 (a real Sovcolor stock now exists; the 1936 entry
  remains its German ancestor).

NEW: SVEMA_FN_64_16MM, SVEMA_FN_64_8MM
  Gauge variants of SVEMA_FN_64, default_format 16mm / 8mm.
  EMULSION NUMBERS DELIBERATELY IDENTICAL to the 35 mm entry -- same
  policy as your 5239/7239 and 8530/8630 pairs. The grain-size and
  resolution difference you asked for comes from magnification, which
  the renderer derives from default_format. Measured on a flat field,
  same emulsion: sigma 0.0131 (35 mm) -> 0.0165 (16 mm) -> 0.0190 (8 mm).
  Inventing different crystal sizes per gauge would double-count this.
  If evidence surfaces that Svema coated a different emulsion for narrow
  gauge, split the numbers then.
  NOTE: your message said "8mm, 16mm, 64mm" -- 64 mm is not a film gauge;
  read as 35 mm (the existing base entry). Correct me if you meant 70 mm.

CHANGED: Soviet B&W spectral response -- weak-red -> EXTENDED-red    [T3]
  SVEMA_FN_64 (all three), SVEMA_FOTO_250, TASMA_FN_64:
    spectral_weights (0.26,0.50,0.24) -> (0.38,0.48,0.14)
    Feature.ORTHO_RESPONSE removed
  Rationale (forum-sourced, from you): Svema/Tasma emulsions descend from
  aerial-survey film sensitised deep into red to cut atmospheric haze.
  Ground-level signature: PALE smooth skin (skin is red-rich), darker
  skies -- the OPPOSITE of the weak-red rendering previously modelled.
  Your pale-skin portrait observation is evidence for extended red; the
  old ortho-ish weights would render skin DARK. Verified in render: skin
  patch vs sky patch differential moved the right way vs HP5 (pre-curve
  mono exposure: skin +12 %, sky -13 %). High contrast was already in the
  curves (gamma 0.86/0.95). Descriptions carry the full AERO LINEAGE note
  marked [T3, forum-sourced]. GEVAERT/LUMIERE keep ORTHO_RESPONSE (they
  are genuinely red-weak); EASTMAN_ORTHO_1930 stays true ortho.

CHANGED: ORWO balance 5500 K -> 4500 K                              [T3]
  ORWOCOLOR_NC21 and NC24. Forum-sourced (you): ORWO colour negative was
  balanced ~4500 K as a deliberate tungsten/daylight compromise
  ("universal"). Commented in both entries; adjust if a datasheet surfaces.

FILES
  film_profiles.py   modified as above (your schema-v2 version)
  cpp_codegen.py     UNCHANGED -- your version already emits every field
                     the new stocks use; nothing needed editing
  film_sim.py        UNCHANGED -- it keys only on UNEVEN_EMULSION; the
                     spectral flip and the new stocks are pure data
  film_profiles.hpp/.cpp   regenerated: 59 stocks, compiles clean under
                     g++ -std=c++14 -Wall -Wextra; DS-4 curve cross-checked
                     C++ vs Python to 6 decimals (D(0.18)=0.571442)

VERIFY SUITE
  Not uploaded, so not run. If your verify.py still asserts a stock count,
  update it: 56 -> 59 (reversal count unchanged at 17).

FOMAPAN 400 ACTION -- CALIBRATED TO THE OFFICIAL DATASHEET (FOMA 04/23)
=======================================================================
You supplied the manufacturer datasheet. Four fields verified; three were
wrong or missing, one confirmed:

1. SPECTRAL SENSITIVITY (your question) -- DID NOT MATCH, fixed.
   The published curve is panchromatic 400-700 nm with sensitivity RISING
   toward the red (peak ~650-680 nm, sharp cut just short of 700) and a
   shallow dip near 450-470. Our previous weights (0.30, 0.48, 0.22) were
   green-dominant -- video-luma-shaped -- and contradicted the curve.
   Band-integrating the published curve gives R >= G >= B:
       spectral_weights  (0.30, 0.48, 0.22)  ->  (0.38, 0.33, 0.29)   [T1]
   Render effect: skin/red fabric lighter (+4.4% mono exposure), sky
   roughly unchanged (+2.4%) -- the curve keeps solid blue sensitivity.

2. GRANULARITY -- was understated 34%, fixed.
   Datasheet: "RMS = 17.5 (Microphen at 20 C, developed to gamma 0.6,
   measured at D = 1.0)". Was 11.5.
       rms_granularity  11.5 -> 17.5                                  [T1]
   Rendered grain sigma now 0.0113 vs HP5's 0.0039 at matched conditions.
   Note the caveat kept in the comment: Foma measured at gamma 0.6, our
   curve is 0.69, granularity rises with development -- so 17.5 is if
   anything conservative.

3. RECIPROCITY -- datasheet Schwarzschild table now modelled.
   Published: 1 s -> 1.5x, 10 s -> 6x, 100 s -> 8x correction. Least-squares
   fit of your single-exponent ReciprocitySpec gives p = 0.637, onset 0.20 s
   (reproduces the table as 1.8x / 4.2x / 9.6x -- the residual is the
   model's shape, documented in the comment; the published failure curve is
   steeper than any single power law). Previously Fomapan fell into the
   generic mono default p = 0.95, far too mild.                 [T1 src/T2 fit]

4. RESOLVING POWER -- added to _RESOLVING_POWER: (55, 90). 90 lp/mm is the
   published figure (contrast unstated, high-contrast by convention); 55 is
   the usual ~0.6x low-contrast estimate, marked as such.       [T1/T2]

CONFIRMED, no change needed: ISO 400 box speed with wide latitude (+1/-2 EV
per the datasheet -- consistent with our "meters closer to 250" note);
gamma 0.69 against Foma's development curves reaching ~0.8+ (medium-contrast
aim ~0.6-0.7); f50 42 c/mm consistent with the 90 lp/mm limit; era; the
profile's existing tier-1 provenance citation of this exact document.

MACO CUBE 400c + PO 100c -- 59 -> 61 STOCKS, BOTH FROM OFFICIAL DATASHEETS
==========================================================================
(Your "CUBE 4C" and "PO 100 C" -- actual product names CUBE 400c / PO 100c,
confirmed by the sheets themselves. Both aliases resolve anyway, including
"cube 4c" and "po 100 c" exactly as you typed them.)

MACO_CUBE_400C                                                       [T1]
  Classic CUBIC crystals in the T-grain era, built as THREE emulsion
  sub-layers of different speed (the VISION3 architecture in a B&W film).
  From the sheet: nominal ISO 400 (effective 100-6400 by developer -- a
  ProcessSpec dimension; profile models the nominal gamma-0.65 state);
  extended panchro 380-730 nm; resolving power 100 lp/mm; AH layer coated
  directly on the base -> halation zero; blue polyester base -> dmin 0.25;
  H&D plot spans 4.2 decades without shouldering -> very long straight line.
  SPECTRAL (your emphasis): the published log-sensitivity curve is BLUE-
  DOMINANT (~3x the mid-band at 400 nm), red plateau with a small ~670 nm
  peak, cliff past ~690, tail to 730. Band-integrated:
      spectral_weights = (0.30, 0.26, 0.44)  -- strong blue, weakest GREEN.
  Reciprocity: the sheet's 7-point table fitted to p=0.711, onset 0.15 s
  (worst residual 26% at 8 s; the staircase is not a power law -- documented).
  Grain [T2]: no granularity figure published; RMS 10.0 estimated for a
  fine cubic 400. Balance 5400 K -- the sheet's own daylight reference.

MACO_PO_100C                                                         [T1]
  ORTHOPANCHROMATIC, ~380-610 nm plus a small residual to ~680: unlike true
  ortho it distinguishes red from black, but red renders DARK -- MACO sold
  it as an unsurpassed portrait film for exactly that etched look.
  From the sheet: ISO 100 daylight / 50-100 tungsten (red-poor sensitising
  costs a stop under red-rich light -- reproduced by the weights, verified
  in render); resolving power 260 lp/mm at 1000:1, the highest of any
  pictorial stock in this database -> f50 118; red filters explicitly not
  recommended; clear base; reversal-processes to a direct positive with
  Dmax past 4.0 (mentioned, not modelled -- entry is the negative state).
  SPECTRAL: flat ~1.0 across 400-580, shoulder at 600, small panchro bump:
      spectral_weights = (0.14, 0.42, 0.44)
  Reciprocity: 6-point sheet table fitted to p=0.587, onset 0.50 s (~18%).
  Grain [T2]: "extremely fine", no figure published; RMS 5.5 estimated.

RENDER VERIFICATION (green display channel, grain off, vs HP5):
                       red     skin    sky
  ILFORD_HP5_PLUS_400  0.496   0.545   0.597
  MACO_CUBE_400C       0.478   0.531   0.644   <- lightest sky (blue-heavy)
  MACO_PO_100C         0.414   0.503   0.652   <- darkest red (ortho-pan)
  Mid-grey anchors exact (0.1800) for both.

Both wired into _PROVENANCE_SOURCES (tier 1, full citations),
_RESOLVING_POWER, and _reciprocity_for. Alphabetical position preserved
(between LUMIERE_LUMICHROME and ORWOCOLOR_NC21). C++ regenerated: 61
stocks, g++ -std=c++14 -Wall -Wextra clean.

HALATION FLAG WARNING -- ROOT CAUSE FIXED, PLUS THE QUESTION IT RAISED
======================================================================
The UserWarning you saw was your own DM-20 audit doing its job: 14 stocks
carried nonzero halation gains with the HALATION flag clear. Under the
declared doctrine (flags derived and non-authoritative), the fix is to
actually DERIVE the flag: _apply_schema_v2 now overwrites the HALATION bit
from halation.active at load time. Disagreement is structurally impossible
for this flag now, for stock #62 too; the audit remains as a regression
guard on the rule itself. Warning gone.

Your follow-up question -- "is a FALSE flag actually true for that film?" --
was the right one. Normalisation makes flag and numbers CONSISTENT, not
TRUE. Audit of the 30 zero-halation stocks:

  INDEFENSIBLE ZEROS, fixed (4):
    ORWOCOLOR_NC24    zero while sibling NC21 carries 0.22/0.12/0.08 on the
                      same base tech -- the review's own example. Now
                      0.15/0.08/0.05 (later stock, better AH, softer not
                      absent).
    AGFA_OPTIMA_100   ordinary colour negatives with normal AH undercoats;
    AGFA_VISTA_200    class peers carry 0.04-0.05. Now 0.040 / 0.045.
    EIGHT_MM_BW       sibling EIGHT_MM_COLOR carries 0.05. Now 0.045 --
                      near-NEUTRAL gains, because a mono stock has no layer
                      stack and its halo is not red-dominant.

  DEFENSIBLE ZEROS, left alone (26): the B&W negatives with effective AH
  undercoats (HP5, APX, Delta, Acros, Fomapan, the Soviet stocks, ...),
  POLAROID_SX70 (reflective integral print -- halation as modelled does not
  apply), MACO_CUBE_400C (the datasheet's specific claim: AH coated directly
  on the base, "preventing reflections rather than weakening them").
  Physically these are all small-but-nonzero in reality; zero here means
  "below modelling threshold", which is the DM-21 absence-vs-zero ambiguity
  in action.

  DECISION LEFT OPEN: a systematic residual floor (e.g. neutral gain ~0.02,
  threshold ~2.2 stops) for those 26 B&W stocks would be more honest
  physically, but it changes highlight rendering on 26 stocks at once and
  activates three blur passes per frame on stocks that currently skip the
  halation stage entirely -- a real render-cost change. Not applied without
  your approval.

PORTRA 400 vs FOUR KODAK E-4040 DATASHEETS (2006 / 2008 / 2009 / 2016)
======================================================================
VERDICT ON "DID THE PRODUCER CHANGE THE FILM 2006-2016?" -- two answers:

1. WITHIN these four sheets: NO property changes for the films they cover.
   2006 vs 2008 vs 2009 are technically identical for all five stocks
   (160NC/VC, 400NC/VC, 800): same ISO/filter tables, same PGI numbers,
   same curve plates (same diagram IDs E4040A..T, same Log H Ref -1.14 /
   -1.44 / -1.74), same MTF plates, same reciprocity statement (no
   correction 1/10,000 s .. 1 s). Only formatting and branding moved.
   PORTRA 800 in the 2016 Kodak Alaris sheet still uses THE SAME measured
   plates (E4040E/S/T/H/M/R, Log H Ref -1.74) and identical PGI (48/70/99
   at 135) -- the 800 emulsion is unchanged 2006->2016 as far as published
   data can show. Differences are non-emulsion: 220 and sheet formats
   dropped, papers list updated, publisher now Kodak Alaris.

2. BUT: none of these sheets documents OUR stock. E-4040 covers the
   pre-2010 generation; plain "PORTRA 400" does not appear in any of the
   four. The 400-speed films here are 400NC and 400VC -- different
   emulsions (laddered Status-M dmin ~0.25/0.65/0.85, PGI 40/42) that were
   DISCONTINUED in 2010 and replaced by the single new PORTRA 400
   documented in publication E-4050, which is what KODAK_PORTRA_400 models
   and cites. So the honest answer to "did Portra 400 change?" is: the
   name survived, the film underneath was REPLACED once (2010); before and
   after that single replacement, the published data shows remarkable
   stability.

ADAPTATIONS APPLIED
  era "1998-present" -> "2010-present" on KODAK_PORTRA_400, with a comment
  recording the four-sheet review. 1998 was the Portra FAMILY launch;
  this profile's numbers and its E-4050 citation are the 2010 product.

VERIFIED CONSISTENT, no change needed
  - reciprocity: our profile carries "no failure" (p=1.0); Kodak states no
    correction needed 1/10,000..1 s across the family, in all four sheets.
  - PGI 4.0-class grain figure remains incommensurable with RMS by
    Kodak's own definition ("cannot be compared to rms granularity" --
    stated verbatim in every sheet); our value stays uncontradicted.
  - the 400NC/VC plates show the laddered Status-M dmin (~0.25/0.65/0.85)
    that the domain review discussed; our neutral_dmin encoding for the
    2010 product remains a documented convention via mask_encoding.

NOT DONE, offered: these sheets fully document PORTRA 800 (curves at
EI 800 / 1600 push-1 / 3200 push-2 -- ready-made ProcessSpec push data --
plus spectral sensitivity, dye density, MTF, PGI). A tier-1 PORTRA_800
profile could be added on request; the push-curve triple would be its
most valuable content.

KODAK_PORTRA_800 ADDED -- 61 -> 62 STOCKS                            [T1]
=========================================================================
Built from all four E-4040 editions (2006/2008/2009/2016, cited in full in
_PROVENANCE_SOURCES -- the only stock in the database backed by four
editions of its own datasheet, which is also the proof of its stability:
the 2016 Alaris sheet reprints the 2006 measured plates unchanged).

From the sheets: ISO 800 daylight (250 @ 3400K/80B, 200 @ 3200K/80A);
curves fitted to the EI 800 plate (Status M, Log H Ref -1.74, gamma
0.60-0.63, B>G>R, long toe); MTF 50% crossings ~55/45/35 c/mm B/G/R with
the >100% adjacency bump; no reciprocity correction 1/10,000..1 s (default
no-failure spec matches the sheet exactly); halation/couplers/base_tint
family-matched to Portra 400; neutral-dmin mask convention like its
siblings (the plate's 0.25/0.65/1.0 Status-M ladder is the orange mask).

GRAIN [T2]: PGI 48 vs Portra 400's 40 at identical viewing = two JND steps
grainier; rms scaled 4.0 -> 5.6. PGI cannot be converted to RMS (Kodak
says so verbatim), hence T2 for grain in an otherwise T1 profile.
Rendered check (256 px, ff35 gauge, mid density): grain sigma 0.00191 vs
Portra 400's 0.00134 -- 1.43x, consistent with a 2-JND PGI step; mid-grey
anchored 0.1800 both.

PUSH DATA NOTED, NOT MODELLED: the sheets publish full curve plates at
EI 1600 (push 1) and EI 3200 (push 2) plus grey-card aim densities for
both. That is ready-made ProcessSpec (DM-12) calibration data -- recorded
in the description so it is not forgotten; this entry is the EI 800
normal-process state.
